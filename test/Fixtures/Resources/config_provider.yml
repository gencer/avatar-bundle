imports:
    - { resource: config.yml }

services:
    provider1:
        class: Maba\Bundle\AvatarBundle\Tests\Fixtures\TestAvatarProvider
        arguments: [provider1]
        tags:
            -  { name: maba_avatar.avatar_provider }
